[build]
  command = "git clone https://github.com/flutter/flutter.git --depth 1 -b stable $HOME/flutter && $HOME/flutter/bin/flutter build web"
  publish = "build/web"

[build.environment]
  FLUTTER_HOME = "/opt/buildhome/.flutter"